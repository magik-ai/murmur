# Orchestration

_Draft outline. To be written in task 15._

The orchestrator's four jobs, talking to lanes, the verification contract for subagents, the model heuristic, capacity and quota checks, never spawn without a go.
