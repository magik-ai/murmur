---
description: Check that this repository is set up to run agents as a team, and report what is missing
---

Check whether this repository is set up to run agents as a team. Use the
`doctor` skill: run the doctor script, lead with the one word status, then
explain in plain sentences only the rows that did not pass, saying what each
one costs me. Treat a row marked optional as an upgrade I have not taken, never
as a fault. Offer the `--fix` run only when a hook script needs to be made
executable or the config file is missing, and say which of the two it will do.
Never push, delete or rename a branch, and never touch a file I wrote.
