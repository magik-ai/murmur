---
description: Set up this repository to run agents as a team, by asking seven questions and writing the files
---

Set this repository up to run agents as a team. Use the `init` skill and follow
it exactly: run the init script's `questions` subcommand, put each unanswered
question to me one at a time with its default shown, store every answer with
the `answer` subcommand as it arrives, then run `apply` and report what was
written, skipped, appended to, or written alongside an existing file. Do not
write any of those files yourself, do not answer a question on my behalf, and
do not change a file that already exists. When it is done, tell me the three
next steps: read the contract, run the doctor, start the first task.
