"""hq - personal agent coordination for a fleet of coding agents.

The CLI lives in `hq.cli`; `bin/hq` and the `hq` console script both call
`hq.cli.main`. Nothing in this package names one organisation: the head office
repo, its owner and the state paths come from `hq.config`.
"""

__version__ = "0.1.0"
