"""hq - names, branch claims and mail for coding agents that share one GitHub login.

The CLI lives in `hq.cli`; `bin/hq` and the `hq` console script both call
`hq.cli.main`. Every setting - the head office repo, its owner, the state
paths - comes from `hq.config`.
"""

__version__ = "0.1.0"
