#!/usr/bin/env python3
"""A fake `loginctl`, so nothing a test runs changes lingering on the real farm."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import _fake  # noqa: E402

_fake.log(["loginctl"] + sys.argv[1:])
sys.exit(0)
