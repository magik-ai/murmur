#!/usr/bin/env python3
"""A fake `tmux`: the kill path still tries a legacy session, and it must not find a real one."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import _fake  # noqa: E402

_fake.log(["tmux"] + sys.argv[1:])
sys.exit(0)
