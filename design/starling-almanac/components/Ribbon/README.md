# Ribbon

A section divider drawn as a thin row of birds that fades out at both ends.

Use `mm-ribbon` between the large parts of a document, a landing page or a report. Never use it between table rows or inside a card: a hairline does that job.
