# Tabs

Sections of one page, one visible at a time.

Words in `ink-muted`, the current one in `ink` with a 2 px rounded `accent` underline. Tabs never navigate away from the page and never carry counts; the four app tabs are the `Nav`.
