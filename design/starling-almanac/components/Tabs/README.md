# Tabs

Sections of one page, one visible at a time.

`mm-tabs` holds buttons or links with `role="tab"` and `aria-selected`. Tab words are `ink-muted`; the current one is `ink` with a 2 px rounded `accent` underline. Tabs never leave the page and never carry counts. The app's three main tabs are the `Nav`, not tabs.
