# Command

A command a person runs in their own terminal, with a Copy button.

Mono on `paper-sunk`, a muted `$`, one command a line, the whole line visible or scrolling inside the block, never wrapped mid-argument. A planned command the farm will run is shown the same way, as a shell line, never as a list of words. Copy writes exactly the text shown.
