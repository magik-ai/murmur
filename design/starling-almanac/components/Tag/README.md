# Tag

A name, not a state: the engine a lane runs on, a project, a region, a model.

`mm-tag` is mono, `ink-muted`, with a `line-strong` hairline and `radius-xs`. A tag never takes a state colour: a state is a `Status`.
