# Tag

A name, not a state: the engine a lane runs on, a project, a region, a model.

Mono, `ink-muted`, a `line-strong` hairline, `radius-xs`. A tag never takes a state colour; a state is a `Status`.
