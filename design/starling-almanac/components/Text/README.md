# Text

A link, a key and a bit of code inside a sentence.

- Links (`mm-link`) are `accent` and underlined. The underline gets thicker on hover. Never add an arrow.
- Keys (`mm-kbd`) are mono in a rounded frame.
- Inline code (`mm-inline-code`) sits on `paper-sunk` and never wraps in the middle of a command.
