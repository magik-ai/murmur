# Text

A link, a key, and a bit of code inside a sentence.

Links are `accent` and underlined (the underline thickens on hover), never an arrow. Keys (`mm-kbd`) are mono in a rounded frame. Inline code (`mm-inline-code`) sits on `paper-sunk` and never wraps mid-command.
